import styles from "./CapabilitiesSection.module.css";

export function CapabilitiesSection() {
  return (
    <section id="capabilities" className={styles.section}>
      <h2>Capabilities</h2>
      <p>
        Explore every game in the X saga, inspect each stage&apos;s boss,
        weakness chain, and collectibles, then compare stage orderings side
        by side.
      </p>
    </section>
  );
}
